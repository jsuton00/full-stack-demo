import React from 'react';
import { NavHeader } from '../common/Navigations';

const Header = () => {
  return (
    <header id="app-header" className="app-header container-fluid">
      <NavHeader />
    </header>
  );
};

export default Header;
