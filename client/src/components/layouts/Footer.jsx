import React from 'react';
import { NavFooter } from '../common/Navigations';

const Footer = () => {
  return (
    <footer id="app-footer" className="app-footer container-fluid">
      <NavFooter />
    </footer>
  );
};

export default Footer;
